<UserControl x:Class="HAP.App.NativeSimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <SolidColorBrush x:Key="InkBrush" Color="#F8FAFC" />
        <SolidColorBrush x:Key="MutedBrush" Color="#94A3B8" />
        <SolidColorBrush x:Key="PanelBrush" Color="#111827" />
        <SolidColorBrush x:Key="CardBrush" Color="#1E293B" />
        <SolidColorBrush x:Key="BorderBrush" Color="#334155" />
        <SolidColorBrush x:Key="AccentBrush" Color="#38BDF8" />
        <SolidColorBrush x:Key="WarnBrush" Color="#F59E0B" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,3" />
        </Style>
        <Style x:Key="ValueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,14,14" />
        </Style>
        <Style x:Key="CommandButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinWidth" Value="104" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Background" Value="#0B1220" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="CaretBrush" Value="{StaticResource InkBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="9,0" />
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Background" Value="#0B1220" />
            <Setter Property="Foreground" Value="#111827" />
            <Setter Property="BorderBrush" Value="#475569" />
            <Setter Property="Height" Value="32" />
        </Style>
        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="Background" Value="#0F172A" />
            <Setter Property="BorderBrush" Value="#334155" />
            <Setter Property="Padding" Value="10,5" />
        </Style>
        <Style x:Key="TopMenuItem" TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="Padding" Value="8,3" />
        </Style>
        <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="Padding" Value="9,4" />
        </Style>
        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="Background" Value="#12324A" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="BorderBrush" Value="#94A3B8" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
        </Style>
        <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#26364F" />
            <Setter Property="Padding" Value="4,2" />
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="MinHeight" Value="24" />
        </Style>
        <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
            <Setter Property="Background" Value="#0B1220" />
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#26364F" />
            <Setter Property="RowBackground" Value="#0F172A" />
            <Setter Property="AlternatingRowBackground" Value="#111827" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>
        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="Foreground" Value="{StaticResource InkBrush}" />
        </Style>
    </UserControl.Resources>

    <DockPanel Background="#0B1220">
        <Menu DockPanel.Dock="Top" Background="#0F172A" Foreground="{StaticResource InkBrush}" Padding="8,0">
            <MenuItem Header="_File" Style="{StaticResource TopMenuItem}">
                <MenuItem Header="Preferences" Click="OnPreferencesClicked" />
                <Separator />
                <MenuItem Header="Exit" Click="OnExitClicked" />
            </MenuItem>
            <MenuItem Header="_Edit" Style="{StaticResource TopMenuItem}">
                <MenuItem Header="Edit Current User" Click="OnEditCurrentUserClicked" />
                <MenuItem Header="Move Reports" Click="OnMoveReportsClicked" />
                <MenuItem Header="Update Distribution Groups" Click="OnUpdateDistributionGroupsClicked" />
                <MenuItem Header="Show/Hide GAL" Click="OnGalVisibilityClicked" />
                <MenuItem Header="Change Manager" Click="OnChangeManagerClicked" />
                <MenuItem Header="Add Delegates" Click="OnAddDelegatesClicked" />
                <MenuItem Header="E-mail Forwarding" Click="OnEmailForwardingClicked" />
            </MenuItem>
            <MenuItem Header="_Tools" Style="{StaticResource TopMenuItem}">
                <MenuItem Header="Device Management" Click="OnOpenDeviceManagementClicked" />
                <MenuItem Header="New User Wizard" Click="OnOpenNewUserWizardClicked" />
                <MenuItem Header="User Lookup" Click="OnOpenUserLookupClicked" />
                <Separator />
                <MenuItem Header="Utilities" Click="OnOpenUtilitiesClicked" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom" Background="#0F172A" Foreground="{StaticResource MutedBrush}" MinHeight="30">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Native console ready." Foreground="{StaticResource MutedBrush}" />
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <TextBlock x:Name="SelectedIdentityStatusText" Text="No selected user" Foreground="{StaticResource MutedBrush}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar x:Name="SearchProgressBar" Width="170" Height="8" Minimum="0" Maximum="100" Visibility="Collapsed" />
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Hybrid Admin Console" FontSize="30" FontWeight="SemiBold" />
                    <TextBlock Text="Dashboard layout foundation | Runtime Profile Wizard ready"
                               Foreground="{StaticResource AccentBrush}"
                               FontSize="13"
                               Margin="0,5,0,0" />
                </StackPanel>
            </Grid>

            <TabControl x:Name="WorkflowTabs" Grid.Row="1" Margin="0,18,0,0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}">
                <TabItem Header="Dashboard">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                        <StackPanel Margin="16">
                            <UniformGrid Columns="4">
                                <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                        <TextBlock Text="Provider Status" Style="{StaticResource SectionTitle}" />
                                        <ItemsControl x:Name="ProviderStatusList" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                        <TextBlock Text="Selected User" Style="{StaticResource SectionTitle}" />
                                        <TextBlock x:Name="DashboardSelectedUserText" Text="-" Style="{StaticResource ValueText}" />
                                        <TextBlock x:Name="DashboardSelectedUserSubText" Text="Search for a user to hydrate dashboard cards." Style="{StaticResource MutedText}" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                        <TextBlock Text="Graph Signals" Style="{StaticResource SectionTitle}" />
                                        <TextBlock x:Name="DashboardGraphText" Text="Not loaded" Style="{StaticResource ValueText}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                        <TextBlock Text="Exchange" Style="{StaticResource SectionTitle}" />
                                        <TextBlock x:Name="DashboardExchangeText" Text="Not loaded" Style="{StaticResource ValueText}" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="Native Migration Coverage" Style="{StaticResource SectionTitle}" />
                                    <DataGrid x:Name="CapabilityGrid" MinHeight="260">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Provider" Width="180" Binding="{Binding Provider}" />
                                            <DataGridTextColumn Header="Capability" Width="260" Binding="{Binding Capability}" />
                                            <DataGridTextColumn Header="Disposition" Width="220" Binding="{Binding Disposition}" />
                                            <DataGridTextColumn Header="Reason" Width="*" Binding="{Binding Reason}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="User Lookup">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SearchBox" Height="40" FontSize="15" Text="amorgan" KeyDown="OnSearchKeyDown" TextChanged="OnSharedSearchTextChanged" />
                            <Button Grid.Column="1" Content="Search" Style="{StaticResource CommandButton}" Width="126" Height="40" Margin="12,0,0,0" Click="OnSearchClicked" />
                        </Grid>

                        <Grid Grid.Row="1" Margin="0,14,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="430" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <DataGrid x:Name="UsersGrid" SelectionChanged="OnUserSelectionChanged">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="SAM" Width="110" Binding="{Binding SamAccountName}" />
                                        <DataGridTextColumn Header="Display Name" Width="170" Binding="{Binding DisplayName}" />
                                        <DataGridTextColumn Header="UPN" Width="220" Binding="{Binding UserPrincipalName}" />
                                        <DataGridTextColumn Header="Source" Width="160" Binding="{Binding Source}" />
                                    </DataGrid.Columns>
                                </DataGrid>
                                <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,12,0,0" Padding="12">
                                    <StackPanel>
                                        <TextBlock Text="Device Match" Style="{StaticResource SectionTitle}" />
                                        <TextBlock x:Name="UserLookupDeviceContextText" Text="No device lookup context." Style="{StaticResource MutedText}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                                <StackPanel>
                                    <UniformGrid Columns="2">
                                        <Border Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="Directory Facts" Style="{StaticResource SectionTitle}" />
                                                <UniformGrid Columns="2">
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Display Name" Style="{StaticResource LabelText}" /><TextBlock x:Name="DisplayNameText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="UPN" Style="{StaticResource LabelText}" /><TextBlock x:Name="UpnText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="SAM" Style="{StaticResource LabelText}" /><TextBlock x:Name="SamText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Mail" Style="{StaticResource LabelText}" /><TextBlock x:Name="MailText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Department" Style="{StaticResource LabelText}" /><TextBlock x:Name="DepartmentText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Title" Style="{StaticResource LabelText}" /><TextBlock x:Name="TitleText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Office" Style="{StaticResource LabelText}" /><TextBlock x:Name="OfficeText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Employee ID" Style="{StaticResource LabelText}" /><TextBlock x:Name="EmployeeIdText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Manager" Style="{StaticResource LabelText}" /><TextBlock x:Name="ManagerText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Direct Reports" Style="{StaticResource LabelText}" /><TextBlock x:Name="DirectReportsText" Text="-" Style="{StaticResource ValueText}" /></StackPanel>
                                                </UniformGrid>
                                                <TextBlock Text="Distinguished Name" Style="{StaticResource LabelText}" />
                                                <TextBlock x:Name="DistinguishedNameText" Text="-" Style="{StaticResource ValueText}" />
                                            </StackPanel>
                                        </Border>

                                        <Border Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="Graph Profile" Style="{StaticResource SectionTitle}" />
                                                <UniformGrid Columns="2">
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Licenses" Style="{StaticResource LabelText}" /><TextBlock x:Name="LicensesText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="PIM Roles" Style="{StaticResource LabelText}" /><TextBlock x:Name="PimRolesText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Risk State" Style="{StaticResource LabelText}" /><TextBlock x:Name="RiskStateText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Last Sign-In" Style="{StaticResource LabelText}" /><TextBlock x:Name="LastSignInText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Password Changed" Style="{StaticResource LabelText}" /><TextBlock x:Name="PasswordChangedText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Methods" Style="{StaticResource LabelText}" /><TextBlock x:Name="GraphMethodsText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                </UniformGrid>
                                            </StackPanel>
                                        </Border>

                                        <Border Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="Authentication Posture" Style="{StaticResource SectionTitle}" />
                                                <UniformGrid Columns="2">
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Default Method" Style="{StaticResource LabelText}" /><TextBlock x:Name="AuthDefaultText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Strength" Style="{StaticResource LabelText}" /><TextBlock x:Name="AuthStrengthText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Conditional Access" Style="{StaticResource LabelText}" /><TextBlock x:Name="ConditionalAccessText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Sign-In Risk" Style="{StaticResource LabelText}" /><TextBlock x:Name="SignInRiskText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="MFA Registered" Style="{StaticResource LabelText}" /><TextBlock x:Name="MfaRegisteredText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Passwordless" Style="{StaticResource LabelText}" /><TextBlock x:Name="PasswordlessText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                </UniformGrid>
                                            </StackPanel>
                                        </Border>

                                        <Border Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="Exchange Mailbox" Style="{StaticResource SectionTitle}" />
                                                <UniformGrid Columns="2">
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Primary SMTP" Style="{StaticResource LabelText}" /><TextBlock x:Name="MailboxText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Recipient Type" Style="{StaticResource LabelText}" /><TextBlock x:Name="RecipientTypeText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Hidden GAL" Style="{StaticResource LabelText}" /><TextBlock x:Name="HiddenGalText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Forwarding" Style="{StaticResource LabelText}" /><TextBlock x:Name="ForwardingText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,14,12"><TextBlock Text="Item Count" Style="{StaticResource LabelText}" /><TextBlock x:Name="ItemCountText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                    <StackPanel Margin="0,0,0,12"><TextBlock Text="Last Logon" Style="{StaticResource LabelText}" /><TextBlock x:Name="LastMailboxLogonText" Text="Not loaded" Style="{StaticResource ValueText}" /></StackPanel>
                                                </UniformGrid>
                                                <TextBlock Text="Addresses" Style="{StaticResource LabelText}" />
                                                <TextBlock x:Name="EmailAddressesText" Text="Not loaded" Style="{StaticResource ValueText}" />
                                            </StackPanel>
                                        </Border>
                                    </UniformGrid>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="180" />
                                            <RowDefinition Height="180" />
                                        </Grid.RowDefinitions>
                                        <Border Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="AD Groups" Style="{StaticResource SectionTitle}" />
                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                    <ItemsControl x:Name="GroupsList" />
                                                </ScrollViewer>
                                            </StackPanel>
                                        </Border>
                                        <Border Grid.Row="1" Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="Distribution Groups" Style="{StaticResource SectionTitle}" />
                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                    <ItemsControl x:Name="DistributionGroupsList" />
                                                </ScrollViewer>
                                            </StackPanel>
                                        </Border>
                                        <Border Grid.Column="1" Grid.RowSpan="2" Style="{StaticResource Card}">
                                            <StackPanel>
                                                <TextBlock Text="Mailbox Delegation" Style="{StaticResource SectionTitle}" />
                                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                    <ItemsControl x:Name="MailboxDelegationList" />
                                                </ScrollViewer>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </TabItem>

                <TabItem Header="Device Management">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="Device Management" FontSize="26" FontWeight="SemiBold" />
                            <TextBlock Text="Search by user, device name, or device ID to review AD, Entra ID, and Intune inventory signals." Style="{StaticResource MutedText}" Margin="0,4,0,12" />
                        </StackPanel>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="DeviceSearchBox" Height="38" Text="amorgan" KeyDown="OnDeviceSearchKeyDown" TextChanged="OnSharedSearchTextChanged" />
                            <Button Grid.Column="1" Content="Search Devices" Style="{StaticResource CommandButton}" Height="38" Margin="12,0,0,0" Click="OnDeviceSearchClicked" />
                        </Grid>
                        <Grid Grid.Row="2" Margin="0,14,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="120" />
                            </Grid.RowDefinitions>
                            <DataGrid x:Name="DevicesGrid" SelectionMode="Single" SelectionChanged="OnDeviceSelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Device" Width="220" Binding="{Binding Name}" />
                                    <DataGridTextColumn Header="OS" Width="150" Binding="{Binding OperatingSystem}" />
                                    <DataGridTextColumn Header="Compliance" Width="130" Binding="{Binding ComplianceState}" />
                                    <DataGridTextColumn Header="Primary User" Width="170" Binding="{Binding PrimaryUser}" />
                                    <DataGridTextColumn Header="Last Check-In" Width="170" Binding="{Binding LastCheckIn}" />
                                    <DataGridTextColumn Header="Source" Width="210" Binding="{Binding Source}" />
                                </DataGrid.Columns>
                            </DataGrid>
                            <StackPanel Grid.Row="1" Margin="0,12,0,8">
                                <Border Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,8">
                                    <TextBlock x:Name="SelectedDeviceText" Text="Selected device: none" Style="{StaticResource MutedText}" />
                                </Border>
                                <WrapPanel>
                                    <Button Content="Reveal BitLocker Key" Style="{StaticResource CommandButton}" Margin="0,0,8,8" Click="OnRevealBitLockerClicked" />
                                    <Button Content="Reveal LAPS Password" Style="{StaticResource CommandButton}" Margin="0,0,18,8" Click="OnRevealLapsClicked" />
                                    <Button Content="Retire from Intune" Style="{StaticResource CommandButton}" Margin="0,0,8,8" Click="OnRetireIntuneDeviceClicked" />
                                    <Button Content="Delete from Intune" Style="{StaticResource CommandButton}" Margin="0,0,8,8" Click="OnDeleteIntuneDeviceClicked" />
                                    <Button Content="Delete from Entra ID" Style="{StaticResource CommandButton}" Margin="0,0,8,8" Click="OnDeleteEntraDeviceClicked" />
                                    <Button Content="Delete from AD" Style="{StaticResource CommandButton}" Margin="0,0,8,8" Click="OnDeleteAdDeviceClicked" />
                                    <Button Content="Delete from All" Style="{StaticResource CommandButton}" Margin="0,0,8,8" Click="OnDeleteAllDeviceClicked" />
                                </WrapPanel>
                            </StackPanel>
                            <TextBox x:Name="DeviceActionOutputTextBox"
                                     Grid.Row="2"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     Text="Select a device, then reveal a protected secret or run a lifecycle action." />
                        </Grid>
                    </Grid>
                </TabItem>

                <TabItem Header="New User Wizard">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,5,0">
                        <Grid Margin="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="460" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="New User Wizard" FontSize="26" FontWeight="SemiBold" />
                                    <TextBlock Text="Enterprise Ready onboarding with validated identity intake, manager lookup, preview, and confirmed provider-backed creation."
                                               Foreground="{StaticResource AccentBrush}"
                                               TextWrapping="Wrap"
                                               Margin="0,6,0,16" />
                                    <UniformGrid Columns="2">
                                        <StackPanel Margin="0,0,12,12"><TextBlock Text="First Name" Style="{StaticResource LabelText}" /><TextBox x:Name="NewUserFirstNameTextBox" Height="32" Text="Jordan" /></StackPanel>
                                        <StackPanel Margin="0,0,0,12"><TextBlock Text="Last Name" Style="{StaticResource LabelText}" /><TextBox x:Name="NewUserLastNameTextBox" Height="32" Text="Casey" /></StackPanel>
                                        <StackPanel Margin="0,0,12,12"><TextBlock Text="SAM Account" Style="{StaticResource LabelText}" /><TextBox x:Name="NewUserSamTextBox" Height="32" Text="jcasey" /></StackPanel>
                                        <StackPanel Margin="0,0,0,12"><TextBlock Text="Manager SAM" Style="{StaticResource LabelText}" /><TextBox x:Name="NewUserManagerTextBox" Height="32" Text="amorgan" /></StackPanel>
                                        <StackPanel Margin="0,0,12,12"><TextBlock Text="Department" Style="{StaticResource LabelText}" /><ComboBox x:Name="NewUserDepartmentComboBox" IsEditable="True" /></StackPanel>
                                        <StackPanel Margin="0,0,0,12"><TextBlock Text="Location" Style="{StaticResource LabelText}" /><ComboBox x:Name="NewUserOfficeComboBox" IsEditable="True" /></StackPanel>
                                    </UniformGrid>
                                    <TextBlock Text="Job Title" Style="{StaticResource LabelText}" />
                                    <TextBox x:Name="NewUserTitleTextBox" Height="32" Text="Operations Analyst" Margin="0,0,0,16" />
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Validate" Style="{StaticResource CommandButton}" Click="OnValidateNewUserClicked" />
                                        <Button Content="Execute" Style="{StaticResource CommandButton}" Margin="10,0,0,0" Click="OnExecuteNewUserClicked" />
                                        <Button Content="Clear" Style="{StaticResource CommandButton}" Margin="10,0,0,0" Click="OnClearNewUserClicked" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="2" Style="{StaticResource Card}">
                                <StackPanel>
                                    <TextBlock Text="Validation and Execution Review" Style="{StaticResource SectionTitle}" />
                                    <TextBlock x:Name="NewUserPreviewText" Text="Validate a request to build the native execution plan." Style="{StaticResource MutedText}" Margin="0,0,0,12" />
                                    <DataGrid x:Name="NewUserPlanGrid" MinHeight="290">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Provider" Width="160" Binding="{Binding ProviderId}" />
                                            <DataGridTextColumn Header="Operation" Width="160" Binding="{Binding Operation}" />
                                            <DataGridTextColumn Header="Blocking" Width="90" Binding="{Binding IsBlocking}" />
                                            <DataGridTextColumn Header="Message" Width="*" Binding="{Binding Message}" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                    <TextBlock Text="Execution Log" Style="{StaticResource LabelText}" Margin="0,14,0,4" />
                                    <ListBox x:Name="NewUserExecutionList" MinHeight="120" Background="#0B1220" Foreground="{StaticResource InkBrush}" BorderBrush="{StaticResource BorderBrush}" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Utilities">
                    <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="430" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Utilities" FontSize="26" FontWeight="SemiBold" />
                                <TextBlock Text="Reusable operational tools for hybrid administration tasks."
                                           Style="{StaticResource MutedText}"
                                           Margin="0,4,0,18" />
                                <Button Content="Sync Hybrid Connection"
                                        Style="{StaticResource CommandButton}"
                                        Height="38"
                                        HorizontalAlignment="Left"
                                        Click="OnSyncHybridConnectionClicked" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource Card}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Utility Status" Style="{StaticResource SectionTitle}" />
                                <TextBox x:Name="UtilityStatusTextBox"
                                         Grid.Row="1"
                                         Text="No utility has been run."
                                         AcceptsReturn="True"
                                         IsReadOnly="True"
                                         VerticalContentAlignment="Top"
                                         TextWrapping="Wrap"
                                         VerticalScrollBarVisibility="Auto"
                                         Margin="0,4,0,0" />
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</UserControl>
