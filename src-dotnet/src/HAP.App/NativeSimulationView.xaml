<UserControl x:Class="HAP.App.NativeSimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Native Simulation"
                           FontSize="24"
                           FontWeight="SemiBold" />
                <TextBlock x:Name="StatusText"
                           Text="Runtime initialized. Search the native simulator below."
                           Margin="0,4,0,0"
                           Foreground="#475569" />
            </StackPanel>
            <Border Grid.Column="1"
                    Background="#E0F2FE"
                    BorderBrush="#7DD3FC"
                    BorderThickness="1"
                    Padding="10,6"
                    VerticalAlignment="Top">
                <TextBlock Text="NativeDotNet"
                           Foreground="#075985"
                           FontWeight="SemiBold" />
            </Border>
        </Grid>

        <Grid Grid.Row="1" Margin="0,20,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox"
                     Height="36"
                     VerticalContentAlignment="Center"
                     Padding="10,0"
                     Text="amorgan" />
            <Button Grid.Column="2"
                    Width="120"
                    Height="36"
                    Content="Search"
                    Click="OnSearchClicked" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <ListView x:Name="UsersList"
                      Grid.Column="0"
                      SelectionChanged="OnUserSelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="SAM"
                                        Width="130"
                                        DisplayMemberBinding="{Binding SamAccountName}" />
                        <GridViewColumn Header="Display Name"
                                        Width="210"
                                        DisplayMemberBinding="{Binding DisplayName}" />
                        <GridViewColumn Header="Source"
                                        Width="180"
                                        DisplayMemberBinding="{Binding Source}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <Border Grid.Column="2"
                    BorderBrush="#D8DEE8"
                    BorderThickness="1"
                    Padding="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock x:Name="SelectedNameText"
                                   Text="Select a user"
                                   FontSize="20"
                                   FontWeight="SemiBold" />
                        <UniformGrid Columns="2" Margin="0,18,0,0">
                            <StackPanel Margin="0,0,18,14">
                                <TextBlock Text="UPN" Foreground="#64748B" />
                                <TextBlock x:Name="UpnText" Text="-" FontWeight="SemiBold" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,14">
                                <TextBlock Text="Department" Foreground="#64748B" />
                                <TextBlock x:Name="DepartmentText" Text="-" FontWeight="SemiBold" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Margin="0,0,18,14">
                                <TextBlock Text="Title" Foreground="#64748B" />
                                <TextBlock x:Name="TitleText" Text="-" FontWeight="SemiBold" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,14">
                                <TextBlock Text="Manager" Foreground="#64748B" />
                                <TextBlock x:Name="ManagerText" Text="-" FontWeight="SemiBold" TextWrapping="Wrap" />
                            </StackPanel>
                        </UniformGrid>

                        <TextBlock Text="Groups"
                                   Margin="0,12,0,6"
                                   Foreground="#64748B" />
                        <ItemsControl x:Name="GroupsList" />

                        <TextBlock Text="Devices"
                                   Margin="0,18,0,6"
                                   Foreground="#64748B" />
                        <ItemsControl x:Name="DevicesList" />

                        <TextBlock Text="Graph Profile"
                                   Margin="0,18,0,6"
                                   Foreground="#64748B" />
                        <TextBlock x:Name="GraphText"
                                   Text="-"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
